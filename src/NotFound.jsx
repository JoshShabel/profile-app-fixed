import PropTypes from "prop-types";

/*TODO: link "learn more" to about*/
const NotFound = () => {
    return (
        <div>
            <p>Not found</p>
            <p>Profiles redirecting here is deliberate. I did not add a profiles page, so I could demonstrate the not found page.</p>
        </div>
    );
};

NotFound.propTypes = {
}

export default NotFound;